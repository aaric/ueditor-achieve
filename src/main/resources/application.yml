# Tomcat settings
server:
    port: 8080
    context-path: /
    compression:
        enabled: true
    tomcat:
        uri-encoding: UTF-8

# Spring settings
spring:
    thymeleaf:
        cache: false
    profiles:
        active: dev
